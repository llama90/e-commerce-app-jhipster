apply plugin: "war"

bootWar {
    mainClassName = "com.github.lucaseo90.store.NotificationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
